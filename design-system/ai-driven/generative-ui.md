# Generative UI Boundaries

## Rules
- Agents compose approved components and tokens only.
- Freeform HTML/CSS is an escape hatch requiring review.
- Cite `rule.*` IDs in agent outputs when making design claims.

## Linked rules
- `rule.ai.generative-boundaries`
